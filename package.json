{
  "name": "italie2cartes-static",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/inject-token.js && node scripts/prepare-public.js",
    "vercel-build": "npm run build"
  }
}
